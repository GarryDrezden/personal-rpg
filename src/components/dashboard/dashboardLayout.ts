/** Flip to `'legacy'` to restore the previous stacked dashboard without a git revert. */
export type DashboardLayoutMode = 'command-bridge' | 'legacy';

export const DASHBOARD_LAYOUT: DashboardLayoutMode = 'command-bridge';
