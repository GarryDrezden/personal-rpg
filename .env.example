# Скопируй в .env в корне проекта (НЕ коммитится)
#
# Dev (Vite proxy /api → :3001) — оставь пустым:
VITE_API_BASE_URL=""

# Production (если API на другом домене/порту):
# VITE_API_BASE_URL="https://api.your-domain.ru"
